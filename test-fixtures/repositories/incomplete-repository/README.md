# Proofline incomplete-repository fixture

Synthetic placeholder README for deterministic repository-verifier smoke tests.
Satisfies rule `required_repository_file_readme` (path `README.md`).
This fixture deliberately omits `figures/results.pdf`.
